#include <stdio.h>
#include <string.h>

char before[260], after[260];
int main()
{
    int rule_num;
    while(scanf("%d\r", &rule_num) == 1)
    {
        if(rule_num == 0) break;
        char find[100], replace[100];
        int i, j, k;
        for(i = 0; i < rule_num; i++)
        {



        }


    }
    return 0;
}
